//template string


// In JavaScript, we can concatenate string just like whe can sum nubers. take a look at the example:

const nome = "Jorge";
const nomeCompleto = nome + " Petrucchio";

console.log(nomeCompleto);

//There is other way to do this. Take a brief look at the exmaple below:

const nomeCompleto_Different = `Meu nome é ${nome}`;
console.log(nomeCompleto_Different);

//Notice that I not using " but ` 
